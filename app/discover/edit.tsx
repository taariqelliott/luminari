import { View, Text } from 'react-native';

export default function DiscoverEdit() {
  return (
    <View className="flex-1 items-center justify-center">
      <Text className="text-2xl font-semibold text-primary">Discover Edit</Text>
    </View>
  );
}
