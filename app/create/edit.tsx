import { View, Text } from 'react-native';

export default function CreateEdit() {
  return (
    <View className="flex-1 items-center justify-center">
      <Text className="text-2xl font-semibold text-primary">Create Edit</Text>
    </View>
  );
}
